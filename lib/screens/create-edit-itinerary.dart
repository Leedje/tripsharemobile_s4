import 'package:flutter/material.dart';

class CreateEditItineraryScreen extends StatelessWidget{

  final itinerary;

  const CreateEditItineraryScreen({required this.itinerary});
  
  @override
  Widget build(BuildContext context) {

    //if itinerary is not null then the screen becomes edit itinerary and performs edit request
    // else it says create and you make a new one
    
    throw UnimplementedError();
  }
}